---
import { freatures } from "../../services/skills";

---
    <div class="relative bg-white text-white py-16 sm:py-24 lg:py-32">
      <div class="mx-auto max-w-md px-4 text-center sm:max-w-3xl sm:px-6 lg:max-w-7xl lg:px-8">
        <h2 class="text-base font-semibold uppercase tracking-wider text-indigo-600">
          Contrate-me
        </h2>
        <p class="mt-2 text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl">
          Tenho as habilidades necessárias para sua empresa ir para o próximo
          nível!
        </p>
        <p class="mx-auto mt-5 max-w-prose text-xl text-gray-500">
          Minha stack atual esta no mundo do Javascript/Typescript, com foco em
          React no frontend, utilizando ferramentas como Vite, Tailwind CSS e no
          backend com Nodejs, Express e Prisma, entre outras que irei conhecer
          no decorrer do caminho.
        </p>
        <!-- <div class="mt-12">
          <div class="grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-3">
            {freatures.map((feature) => (
              <div class="pt-6">
                <div class="flow-root rounded-lg bg-gray-50 px-6 pb-8">
                  <div class="-mt-6">
                      <span class="h-10 w-10 inline-flex justify-center items-center rounded-md bg-indigo-500 p-3 shadow-lg">
                          <feature.icon/>
                      </span>
                    <h3 class="mt-8 text-lg font-medium tracking-tight text-gray-900">
                      {feature.name}
                    </h3>
                    <p class="mt-5 text-base text-gray-500">
                      {feature.description}
                    </p>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div> -->
      </div>
    </div>
